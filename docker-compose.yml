services:
  web:
    build: .
    image: wedding-reservations-web:latest
    hostname: wedmainapp
    ports:
      - "8081:8080"
    environment:
      EXPO_PUBLIC_API_URL: ${EXPO_PUBLIC_API_URL:-/api}
    networks:
      - app
    restart: unless-stopped

networks:
  app:
